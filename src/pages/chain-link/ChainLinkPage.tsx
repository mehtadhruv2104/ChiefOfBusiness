import { Fence } from 'lucide-react'
import { PageStub } from '@/components/ui/PageStub'

export default function ChainLinkPage() {
  return (
    <PageStub
      icon={Fence}
      title="BSWI (Chain Link)"
      description="The boundary, height, wire diameter and mesh size estimator is coming in the next build phase."
      phase="Phase 3"
    />
  )
}
