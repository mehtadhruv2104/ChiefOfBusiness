import { Hammer } from 'lucide-react'
import { PageStub } from '@/components/ui/PageStub'

export default function BharatFabricoPage() {
  return (
    <PageStub
      icon={Hammer}
      title="Bharat Fabrico"
      description="The quotation builder — client details, material table, charges and PDF export — is coming in a later build phase."
      phase="Phase 4"
    />
  )
}
