import { Settings } from 'lucide-react'
import { PageStub } from '@/components/ui/PageStub'

export default function SettingsPage() {
  return (
    <PageStub
      icon={Settings}
      title="Settings"
      description="Company profile, GST, bank details, terms and logo — used to populate every quotation — are coming with the Bharat Fabrico module."
      phase="Phase 4"
    />
  )
}
